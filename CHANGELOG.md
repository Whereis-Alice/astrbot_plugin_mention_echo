# 更新日志

本文件记录「艾特回声 / Mention Echo」的所有重要变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [v1.0.0] - 2026-09-06

首个正式版本。基于 [astrbot_plugin_who_at_me_pro](https://github.com/xiaoruange39/astrbot_plugin_who_at_me_pro)
v1.0.4 重构而来，独立命名空间，可与原插件共存于同一目录（但**不要同时启用**，命令会打架）。

### 新增

- **自动清理与存储治理**（对应上游 issue #16「占用空间越来越大」）
  - 后台巡检任务：启动 60 秒后首跑，之后每 6 小时一次，不阻塞消息处理。
  - 按时间过期：艾特记录默认保留 30 天，成员昵称缓存保留 14 天，渲染临时图保留 24 小时。
  - 三层容量封顶：`renders/` 64 MB、`message_images/` 256 MB、受管目录合计 512 MB，
    超出即从最旧的开始删，`0` 表示不限制。
  - 孤儿数据回收：清掉不再被任何索引引用的历史 KV 键，并顺手校正索引；只动本插件自己的键。
  - 单轮限流：一次巡检最多删 2000 个 KV 键，避免长时间占住数据库写锁。
  - 宽限期保护：刚落盘（渲染中 / 下载中）的文件不参与任何删除，宁可暂时超配额。
  - `resources/`（用户上传的字体与自定义图片）永不删除。
- **`艾特存储` 命令**：一份存储体检报告——各目录体积、记录条数、距离配额还剩多少、上次巡检结果。
- **`艾特清理` 命令**：管理员手动触发一次完整巡检，立刻回报释放了多少空间。
- **`艾特排行榜 [天数]`**：统计最近 N 天谁 @ 你最多，纯文本输出，不出图、不额外占空间。
- **可选的 LLM 工具**（`mention_echo_recent_mentions`）：允许大模型在自然语言提问时代查最近的艾特记录。
  **默认关闭**，涉及隐私，开启前请阅读 README 对应章节。
- **平滑迁移**：自动继承「谁艾特我 / 谁艾特我 Pro」的历史记录与外观设置，触发词保持兼容。
- **插件 Logo**：新增 `logo.svg` / `logo.png`。
- **清理逻辑单元测试**：`tests/test_maintenance.py`，不依赖 pytest，`python tests/test_maintenance.py` 直接跑。
  清理是本插件唯一「不可逆删用户文件」的部分，因此单独覆盖到位。

### 变更

- **渲染改为 t2i 优先**：默认调用 AstrBot 的 t2i 出图服务，不再强制依赖本机 Chromium，
  机器人进程内存占用显著下降；t2i 失败自动降级本地浏览器 → 合并转发纯文本 → 纯文本提示，四层兜底。
- **t2i 失败冷却**：连续失败后冷却 180 秒再重试，避免每次查询都白等一轮超时。
- **多页出图并发化**：记录多到需要分页时，多张图并发向 t2i 发请求（上限 3 页），比逐页串行快得多，同时不至于打满出图服务。
- **配置项全面重整**：43 个配置项按 `global` / `feature` / `record` / `reminder` / `render` /
  `maintenance` / `message` 七组归类，每项都有中文说明与默认值。
- **代码拆分**：单文件拆为 `main.py` + `modules/`（config / data / message / rendering /
  maintenance / page_api / page_settings），便于定位与后续维护。
- **全部标识符改名**，与原插件互不冲突：插件名、类名、数据目录、KV 键前缀、Web API 路由、
  渲染文件名前缀、LLM 工具名。

### 修复

- 渲染临时图不再无限堆积：出图时做一次节流过的就近清理，容量与总配额兜底交给后台巡检。
- 手工维护的索引与实际 KV 键集合失配时会自动重建，不再出现「记录还在但查不到」。
- 运行时字典（KV 锁、群管道锁、群活跃时间）不再无限增长，超过阈值即回收空闲项。
- 图片缓存条目在本地文件已被删除时会自动丢弃，渲染时按原始 URL 重新下载，而不是显示破图。
- 权限门禁收紧：破坏性命令（`清除全部艾特数据` / `艾特清理`）要求机器人管理员，
  群级配置命令要求群管理员，只读命令不设门禁。

[Unreleased]: https://github.com/Whereis-Alice/astrbot_plugin_mention_echo/compare/v1.0.0...HEAD
[v1.0.0]: https://github.com/Whereis-Alice/astrbot_plugin_mention_echo/releases/tag/v1.0.0