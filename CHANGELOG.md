# 更新日志

本文件记录「艾特回声 / Mention Echo」的所有重要变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [v1.3.0] - 2026-09-08

主题：**让“补课上下文”真的能存下来，查询不再刷屏。**

### 修复

- **修复“明明开了上下文，查询却没有上下文”的配置陷阱。**
  `提醒截图上下文` 原本只作用于回群提醒队列，和手动 `谁艾特我` 的记录完全独立；
  `查询时是否连带艾特前后的群聊记录 = 始终展示` 原本也只改变显示，并不会保存任何内容。
  现在选择 `始终展示` 后，插件会从设置生效后的新消息开始自动采集查询上下文并在查询图中展示。
  旧记录没有当时的群消息，无法逆向补齐，这是数据边界而不是渲染问题。
- **修复一次艾特被解析两次。** 上下文开启时，同一消息曾先为前后文解析一次、再为正式记录解析一次，
  导致重复的 `record image diagnostic` 日志，也可能重复做图片解析工作。现在正式记录复用第一次结果，
  同时保持前文缓存不被改写。
- **查询成功只发结果图片。** 移除等待提示和“最近几次 / 共多少条”的长文字摘要，避免群里刷屏；
  只有出图失败时才走合并转发纯文本兜底。
- **图片诊断日志默认关闭。** `record image diagnostic` / `query image diagnostic` 改为由
  `feature.image_diagnostics_enabled` 控制，默认 `false`；排查图片问题时才需要临时开启。
- **提醒上下文异常配置不再中断记录。** 前后文条数写成空值、文字或负数时会安全回落并夹取到允许范围。
- **普通上下文消息不再无条件落原图文件。** 只有正式艾特记录或正在补充已记录艾特后文的消息才会保留本地原图；
  其余滚动前文只保留 URL / 过期占位，避免开启上下文后被日常群聊图片占满缓存。
- **修复共享缓存图被提前删除。** 正式记录、多个被 @ 对象和待发提醒可以安全共用同一个图片引用；
  其中一条被清理或提醒送达时只会断开自己的引用，未被任何记录使用的文件改由自动巡检回收。
- **缓存过期不再静默缺图。** `mecache://` 指向的文件已被保留期或配额巡检删除时，出图前会自动变为
  “图片已过期”占位，而不是在截图里悄悄少掉一张图。

### 兼容性

- 补齐标准 OneBot v11、LLOneBot / LLBot 的 `at`、`image`、群/用户/机器人 ID 与合并转发动作兼容。
- 支持 SnowLuma 一类以 JSON 字符串传递 `messageChain`、使用 `mention_user` / `imageUrl` /
  `senderId` / `botId` 的事件形态。
- OneBot API 调用兼容 `bot`、`bot.api`、`bot.client` 上的同步或异步 `call_action` / `call_api`，
  以及只接受关键字 `action` 的实现；多页图片转发会从 `send_group_forward_msg` 回退到
  `send_forward_msg`。

### 测试

- 新增 OneBot 协议兼容自测，覆盖上述消息段、同步关键字 API 和通用转发回退；CI 已接入。
- 查询自测新增“只解析一次”和“成功时只发图”回归用例。

## [v1.2.0] - 2026-09-06

主题：**两条查询命令合并成一条**。

`谁艾特我` 和 v1.1.0 新加的 `艾特回顾` 做的其实是同一件事——看谁艾特了我，
区别只在「看几条」和「要不要带上下文」。两条命令等于两套代码路径、两份文档、两处踩坑，
本版把它们并成一条：**条数写在命令里，上下文由配置项决定**。

### 变更

- **`艾特回顾` 不再是独立命令，降级成 `谁艾特我` 的别名。**
  `艾特回顾` / `回顾艾特` / `艾特补课` / `at_recap` / `catch_up` 一律等价于 `谁艾特我`，
  默认查自己，命令里带 @ 就查对方（`艾特回顾 @张三` 等于 `谁艾特他 @张三`）。
  旧写法一个都没废弃，群友的肌肉记忆不用改。
- **条数直接跟在命令后面**：`谁艾特我 3` = 只看最近 3 次艾特，单次上限 50。
  `谁艾特我3次`、`谁艾特我 12 条` 同样认。不写数字就是老行为：全部记录、分页出图。
- **带条数时少发一条消息**：以前是先回一句「稍等」再发图；现在换成一行摘要
  （这次看了几条 / 一共存了多少条 / 最近一次是多久前）和图片一起发出去。
- 原 `艾特回顾` 写死的「最多 5 次、强制带上下文」限制取消，交给下面两个配置项。

### 新增

- 配置项 **`查询时是否连带艾特前后的群聊记录`（`query_context_mode`）**，三态：
  `跟随群设置`（默认，只有发过 `开启艾特上下文` 的群才展示）、
  `始终展示`（存了就展示，适合久不看群补课）、
  `从不展示`（只看被艾特那一条，出图最小）。
  这个开关**只管查询**；回群自动提醒继续跟随群设置，互不影响。
- 配置项 **`查询默认只看最近多少次艾特`（`query_recent_count`）**，默认 0 = 全部记录。
  设成 3 就等于把 `谁艾特我` 永久变成补课视图；命令里写的数字优先级更高。
- **查询链路单元测试** `tests/test_query.py`：11 个用例 / 约 100 处断言，零外网、零第三方依赖，
  覆盖正则的各种写法与误伤（`谁在艾特我`、`艾特回顾一下` 不该触发）、别名的目标推断、
  条数的取值与夹取，以及三态上下文在「出图」和「放不下时裁剪」两条路径上是否一致。CI 已接入。

### 修复

- **纯文本模式下 `艾特回顾` 看不到上下文。** v1.1.0 只在出图路径上强制展开 `before` / `after`，
  合并转发与纯文本降级路径仍按群设置判断，同一条命令在两种输出下结果不一样。
  现在三条输出路径共用同一个判定。
- **`艾特回顾` 超出页数上限时会整条丢记录。** v1.1.0 直接按 `单次查询最多几页` 截断，
  被截掉的艾特就此消失；现在和 `谁艾特我` 走同一套「先减上下文、再减记录」的取舍，
  最近的那几次艾特一定看得到。

## [v1.1.0] - 2026-09-06

主题：**把图片从数据库里搬出去**。

「这类插件跑久了占用上 G」的根因不在磁盘目录，而在数据库——记录里直接躺着图片的 base64 原文。
本版把存储改成「数据库只存引用、图片字节只落磁盘」，同一条带图记录从 **780 KB 降到 931 字节**。
另外新增「艾特回顾」，专门给久不看群的人补课。

### 新增

- **`艾特回顾 [次数]`**：一次看完最近 1–5 次艾特，并且**每一次都强制带上当时的群聊上下文**。
  久不看群时用它比 `谁艾特我` 更合适——后者只给你 @ 那一句，回顾会把前后的对话一起还原出来。
  同义词：`艾特补课`、`回顾艾特`、`at_recap`、`catch_up`。
- **图片引用式存储**：数据库里从此只允许出现三种轻量引用——http(s) 链接、
  `mecache://<相对路径>`、或 `mecache://expired` 占位，单条引用硬性截断在 1024 字符。
  写库前有一道统一闸门，任何字段（含嵌套的 `quote` / `before` / `after` / 卡片封面）
  塞进超长原文都会被就地剥离，纯文本字段也顺带截断在 2000 字符。
- **维护期数据库瘦身**：每轮巡检顺手扫一遍历史记录，把老版本残留在库里的图片原文剥掉。
  释放量单独统计，`艾特存储` 和 `艾特清理` 的报告里都能看到「数据库瘦身」这一行。
  **已经膨胀到 1 GB 的旧库，装上本版会被逐轮自动瘦下来，不需要手动清空数据。**
- **存储层单元测试** `tests/test_storage.py`：13 个用例 / 56 处断言，零外网、零第三方依赖，
  覆盖下面两个上游 bug 的回归、5 层嵌套字段、`../` 越界路径拒绝、留盘窗口的磁盘上限。

### 变更

- **`最近多少条记录缓存图片` 默认 0 → 20。** 以前设 0 是为了躲开「落盘就等于重复存两份」，
  现在原文只存一份、且只在磁盘上，就没必要躲了。之所以还要留 20 条而不是全靠链接：
  **QQ 图片链接带 rkey，会轮换失效**，只留链接的话隔天再查就是一张破图。
- **配额收紧**：`message_images` 256 MB → **128 MB**，受管目录总配额 512 MB → **256 MB**。
  数据库不再存图之后，原来的上限没必要开那么宽。
- 渲染链路与降级路径同步认识 `mecache://` 引用；本地文件不在了就按原始链接现取现下，不显示破图。

### 修复

以下两个存储 bug 来自上游，v1.0.0 沿用了上游的结构因而同样存在，本版修掉：

- **同一张图存了两遍。** `_cache_image` 把整段 base64 原文写进 `image_cache.source`，
  而 `images` 里原本那份从来没被替换掉——一张 4 MB 的图在同一条 JSON 里占 8 MB。
  现在两处都只存引用。
  （注：该字段默认不生成，所以这一半通常不显形，真正吃掉硬盘的是下面这条。）
- **清理只清了一半。** `_drop_record_image_cache` 只 `pop("image_cache")`，`images` 字段永不释放。
  配合「每人每群最多 300 条」，等于每个「群 + 人」组合最多攒 300 条带原图的记录，永远不回收。
  同时旧的自动清理只扫磁盘目录和记录条数，**完全不管数据库里的这份膨胀**。
  现在过期释放走统一的引用化路径，两处一起处理，历史数据也会被巡检追认。
- 顺带修掉本版自己在合并转发 / 卡片封面上重犯的同类问题：封面已经引用化之后又被下载了第二份文件。
  新增的存储测试就是抓到它的那一个。

## [v1.0.0] - 2026-09-06

首个正式版本。基于 [astrbot_plugin_who_at_me_pro](https://github.com/xiaoruange39/astrbot_plugin_who_at_me_pro)
v1.0.4 重构而来，独立命名空间，可与原插件共存于同一目录（但**不要同时启用**，命令会打架）。

### 新增

- **自动清理与存储治理**（对应上游 issue #16「占用空间越来越大」）
  - 后台巡检任务：启动 60 秒后首跑，之后每 6 小时一次，不阻塞消息处理。
  - 按时间过期：艾特记录默认保留 30 天，成员昵称缓存保留 14 天，渲染临时图保留 24 小时。
  - 三层容量封顶：`renders/` 64 MB、`message_images/` 256 MB、受管目录合计 512 MB，
    超出即从最旧的开始删，`0` 表示不限制。
  - 孤儿数据回收：清掉不再被任何索引引用的历史 KV 键，并顺手校正索引；只动本插件自己的键。
  - 单轮限流：一次巡检最多删 2000 个 KV 键，避免长时间占住数据库写锁。
  - 宽限期保护：刚落盘（渲染中 / 下载中）的文件不参与任何删除，宁可暂时超配额。
  - `resources/`（用户上传的字体与自定义图片）永不删除。
- **`艾特存储` 命令**：一份存储体检报告——各目录体积、记录条数、距离配额还剩多少、上次巡检结果。
- **`艾特清理` 命令**：管理员手动触发一次完整巡检，立刻回报释放了多少空间。
- **`艾特排行榜 [天数]`**：统计最近 N 天谁 @ 你最多，纯文本输出，不出图、不额外占空间。
- **可选的 LLM 工具**（`mention_echo_recent_mentions`）：允许大模型在自然语言提问时代查最近的艾特记录。
  **默认关闭**，涉及隐私，开启前请阅读 README 对应章节。
- **平滑迁移**：自动继承「谁艾特我 / 谁艾特我 Pro」的历史记录与外观设置，触发词保持兼容。
- **插件 Logo**：新增 `logo.svg` / `logo.png`。
- **清理逻辑单元测试**：`tests/test_maintenance.py`，不依赖 pytest，`python tests/test_maintenance.py` 直接跑。
  清理是本插件唯一「不可逆删用户文件」的部分，因此单独覆盖到位。

### 变更

- **渲染改为 t2i 优先**：默认调用 AstrBot 的 t2i 出图服务，不再强制依赖本机 Chromium，
  机器人进程内存占用显著下降；t2i 失败自动降级本地浏览器 → 合并转发纯文本 → 纯文本提示，四层兜底。
- **t2i 失败冷却**：连续失败后冷却 180 秒再重试，避免每次查询都白等一轮超时。
- **多页出图并发化**：记录多到需要分页时，多张图并发向 t2i 发请求（上限 3 页），比逐页串行快得多，同时不至于打满出图服务。
- **配置项全面重整**：43 个配置项按 `global` / `feature` / `record` / `reminder` / `render` /
  `maintenance` / `message` 七组归类，每项都有中文说明与默认值。
- **代码拆分**：单文件拆为 `main.py` + `modules/`（config / data / message / rendering /
  maintenance / page_api / page_settings），便于定位与后续维护。
- **全部标识符改名**，与原插件互不冲突：插件名、类名、数据目录、KV 键前缀、Web API 路由、
  渲染文件名前缀、LLM 工具名。

### 修复

- 渲染临时图不再无限堆积：出图时做一次节流过的就近清理，容量与总配额兜底交给后台巡检。
- 手工维护的索引与实际 KV 键集合失配时会自动重建，不再出现「记录还在但查不到」。
- 运行时字典（KV 锁、群管道锁、群活跃时间）不再无限增长，超过阈值即回收空闲项。
- 图片缓存条目在本地文件已被删除时会自动丢弃，渲染时按原始 URL 重新下载，而不是显示破图。
- 权限门禁收紧：破坏性命令（`清除全部艾特数据` / `艾特清理`）要求机器人管理员，
  群级配置命令要求群管理员，只读命令不设门禁。

[Unreleased]: https://github.com/Whereis-Alice/astrbot_plugin_mention_echo/compare/v1.3.0...HEAD
[v1.3.0]: https://github.com/Whereis-Alice/astrbot_plugin_mention_echo/releases/tag/v1.3.0
[v1.2.0]: https://github.com/Whereis-Alice/astrbot_plugin_mention_echo/releases/tag/v1.2.0
[v1.1.0]: https://github.com/Whereis-Alice/astrbot_plugin_mention_echo/releases/tag/v1.1.0
[v1.0.0]: https://github.com/Whereis-Alice/astrbot_plugin_mention_echo/releases/tag/v1.0.0
